// DEPRECATED & REMOVED:
// MockDataService has been completely removed in favor of real MongoDB backend
// repositories and Provider state management (AuthProvider, FarmProvider,
// DesignProvider, SuitabilityProvider, EconomicsProvider, ReportProvider,
// DashboardProvider, NotificationProvider, SupportProvider, SettingsProvider).
